<%= form_with model: article do |f| %>
  <div>
    <%= f.label :title %>
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <%= f.submit(class: "btn btn-primary") %>
<% end %>
